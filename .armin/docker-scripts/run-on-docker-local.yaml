# Local deploy settings — scripts read this file only (no CLI -- flags).

stack_name: geoquiz
image_tag: geoquiz-api:latest
compose_file: ../../docker-compose.yml
dockerfile: ../../Dockerfile
docker_network: t3-net
internal_port: "8080"
publish_port: "8094"
delete_volume: "no"
delete_image: "yes"
